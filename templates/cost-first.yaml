# Ainfera Routing — cost-first tenant policy (starter template)
policy_name: cost_first
cost_weight: 0.50
quality_weight: 0.30
latency_weight: 0.20
fallback_enabled: true
fallback_cost_penalty_pct: 5.0
compliance_veto_threshold: 0.50
notes: >
  Minimize cost within ATS quality floor. Use X-Ainfera-Priority: cost on hot paths.
